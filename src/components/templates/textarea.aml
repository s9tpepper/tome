overflow [id: "container", axis: "vertical", clamp: true]
  zstack
    text [id: "contents", foreground: foreground] state.display_input
    if state.focused == true
      text state.cursor_prefix
        span [foreground: state.background, background: state.foreground] state.cursor_char
